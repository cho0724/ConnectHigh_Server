import 'package:flutter/material.dart';

class StudyBoard extends StatelessWidget {
  const StudyBoard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('입시 게시판'),),
    );
  }
}