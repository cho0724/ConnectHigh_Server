import 'package:flutter/material.dart';

class LoveBoard extends StatelessWidget {
  const LoveBoard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('연애 게시판'),),
    );
  }
}